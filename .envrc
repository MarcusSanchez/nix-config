# The repo's operational scripts (bin/secrets:edit, age:place, ...) on PATH
# while cd'd in. They're plain executables — `./bin/age:place` works without
# direnv too. devenv was retired from this repo on 2026-08-01: four shell
# scripts don't need a 67 MiB profile and a second lockfile.
PATH_add bin
